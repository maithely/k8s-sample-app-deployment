
**ha-configuration-guide.md:**
```markdown
# High Availability Configuration Guide

To configure the nginx application for high availability, set the `replicaCount` in the `values.yaml` file to more than 1. This ensures multiple instances of the application are running across different nodes.
